services:
  bot:
    build: .
    container_name: dj_monaco_bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    dns:
      - 1.1.1.1
      - 8.8.8.8
    volumes:
      - ./data:/app/data
